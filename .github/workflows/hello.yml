name: Hello Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main    

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello World"
